import { FC } from 'react';
import useStyles from './useStyles';

const AddColumnWidget: FC = (): JSX.Element => {
  const classes = useStyles();
  return (
    <div className={classes.widget}>
       
    </div>
  );
};

export default AddColumnWidget;