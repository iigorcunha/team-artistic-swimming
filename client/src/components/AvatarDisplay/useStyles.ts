import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(() => ({
  root: {
    margin: 0,
    padding: 0,
    marginRight: 30,
  },
  avatarContainer: {
    margin: 0,
    padding: 0,
  },
  iconButton: {
    margin: 0,
    padding: 0,
  },
  avatar: {
    width: 50,
    height: 50,
  },
}));

export default useStyles;
