import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    margin: 0,
    padding: 0,
    width: 22,
    height: 22,
  },
}));

export default useStyles;
