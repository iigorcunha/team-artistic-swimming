import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  dialogContainer: {
    alignSelf: 'start',
  },
  addCardButton: {
    width: 100,
    color: '#818ba1',
  },
}));

export default useStyles;
